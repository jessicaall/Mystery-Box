# Decided to use randint to randonly generate all four of the numbers 1, 2, 3, 4

import random


NUM_TRIALS = 10

for item in range(0, NUM_TRIALS):

    # randint finds numbers between endpoints, including the endpoints
    prizze_num = randome.randint(1,4)
    print(prize_num)
